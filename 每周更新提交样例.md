# PatPat EDM 周报 - 每周更新提交样例

给同事时，直接照这个格式准备即可。

## 一、每周必须提供的 4 项内容

### 1. 上一版已确认周报 HTML（baseline）
作用：作为本周追加的基线，保证历史周不被改写。

示例：
- `patpat_edm_weekly_dashboard_2026-05-10_2026-05-16_v70_email_app_revenue_adjusted.html`

### 2. Emarsys 手动邮件周报
作用：手动邮件的 Send / Delivered / Opened / Clicked / Unsubscribed 来源。

文件示例：
- `emarsys手动邮件-TrendReporting5.17-5.23.csv`

### 3. Emarsys 自动邮件周报
作用：自动邮件的 Send / Delivered / Opened / Clicked / Unsubscribed 来源。

文件示例：
- `emarsys自动邮件-TrendReporting5.17-5.23.csv`
- 如果拿到的是 `.xlsx` 也可以，例如：
- `emarsys自动邮件-TrendReporting5.17-5.23.xlsx`

### 4. Attribuly 归因数据
作用：Revenue / UV / Order UV / 大盘 / Email / SMS / 私域合计 来源。

文件示例：
- `attribuly归因数据表5.17-5.23.xlsx`
- 或：`attribuly归因数据表5.17-5.23.csv`

---

## 二、发给 Codex 时建议直接这样写

### 最简版本
```text
请按 patpat-edm-weekly-updater skill 更新周报。

本周周期：2026-05-17 ~ 2026-05-23
基线周报：patpat_edm_weekly_dashboard_2026-05-10_2026-05-16_v70_email_app_revenue_adjusted.html
新增文件：
1. emarsys手动邮件-TrendReporting5.17-5.23.csv
2. emarsys自动邮件-TrendReporting5.17-5.23.csv
3. attribuly归因数据表5.17-5.23.xlsx

要求：append-only，不覆盖历史周，输出新版本 HTML + changelog。
```

### 如果有特殊口径调整，要额外补这一段
```text
本周补充口径：
1. Email Revenue 需额外加上 App 端出单 XX.XX
2. 自动邮件欢迎邮件新增 campaign：xxxxxx
3. 某个 flow family 白名单有调整：xxxxxx
4. 某个 theme 需要拆分展示：xxxxxx
```

---

## 三、最容易漏掉的说明（必须补充）

如果本周有下面任何一种变化，一定要一起说：

1. **Email Revenue 手工修正**
- 例如：App 端出单补量
- 这会影响：
  - Email Revenue
  - 私域合计 Revenue
  - Email Revenue占比
  - 私域Revenue占比

2. **自动邮件白名单变化**
- 例如新增欢迎邮件 campaign：
  - `eautomation_Sale_welcome_us-1_Miki_2026_2509`
- 这会影响：
  - 自动邮件总 Revenue
  - 自动邮件 Order UV / UV
  - By Flow 的欢迎邮件 Revenue

3. **手动邮件 theme 拆分 / 特殊展示**
- 例如 `star` 要从 `sale` 里单独拆出来展示

4. **周期修正**
- 例如之前给错成 `4.12-4.19`，后来修正为 `4.12-4.18`
- 这种必须明确说明，不然历史比较会错位

---

## 四、文件格式最低检查

### Emarsys 文件
至少应包含：
- `Launch date`
- `Campaign name`
- `Total sent`
- `Total delivered`
- `Total opened`
- `Clicked`
- `Total unsubscribed`
- 最后一行 total row

### Attribuly 文件
至少应包含：
- `Campaign(Source/Medium)`
- `Channel`（如有）
- 或 `Medium`
- `Clicks(UV)`
- `Attribuly tracked conversions`
- `Attribuly tracked conversion value`

---

## 五、更新成功后的标准产出

每次更新后应产出：

1. 新版周报 HTML
2. 对应 changelog txt
3. 如该版本生成了 audit，也一起保留

示例：
- `patpat_edm_weekly_dashboard_2026-05-17_2026-05-23_v71_append_only.html`
- `patpat_edm_weekly_dashboard_2026-05-17_2026-05-23_v71_append_only_changelog.txt`

---

## 六、一句话原则

**每周更新只做“追加新周 + 保持历史稳定”，不要重算和覆盖旧周。**
